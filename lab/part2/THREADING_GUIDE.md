# คำแนะนำการทดสอบ Multi-threading และ Race Condition

## 📋 ไฟล์ที่เกี่ยวข้อง

### 2.3 Multi-threading
- `multi_threading_demo.py` - แสดงการทำงานของ threads

### 2.4 Race Condition  
- `race_condition_problem.py` - แสดงปัญหา Race Condition
- `race_condition_fixed.py` - แสดงวิธีแก้ไข
- `race_condition_comparison.py` - เปรียบเทียบผลลัพธ์

---

## 🚀 วิธีการรัน

### 1. Multi-threading Demo

```bash
python3 multi_threading_demo.py
```

**สิ่งที่จะเห็น:**
- Worker threads ทำงานพร้อมกัน
- การดาวน์โหลดไฟล์แบบ parallel
- Thread IDs และข้อมูล threading

**สิ่งที่ควรสังเกต:**
- ลำดับการทำงานไม่แน่นอน (แต่ละครั้งอาจต่างกัน)
- เวลารวมน้อยกว่าการทำงานแบบเรียงลำดับ
- Threads ทำงานพร้อมกัน (concurrent)

---

### 2. Race Condition Problem

```bash
python3 race_condition_problem.py
```

**สิ่งที่จะเห็น:**
- ค่าที่คาดหวัง vs ค่าที่ได้จริง
- ส่วนต่าง (ค่าที่สูญหาย)
- ทดสอบ 3 ครั้ง เพื่อให้เห็นว่าผลไม่คงที่

**ตัวอย่างผลลัพธ์:**
```
ค่าที่คาดหวัง: 5000
ค่าที่ได้จริง: 4723
ส่วนต่าง: 277
❌ เกิด Race Condition!
```

**ทำไมผลต่างกันแต่ละครั้ง?**
- ขึ้นอยู่กับ timing ของ threads
- ขึ้นอยู่กับการ schedule ของ OS
- แสดงให้เห็นว่าเป็น non-deterministic bug

---

### 3. Race Condition Fixed

```bash
python3 race_condition_fixed.py
```

**สิ่งที่จะเห็น:**
- ค่าถูกต้อง 100%
- ใช้ Lock เพื่อป้องกัน
- อธิบายวิธีแก้ไข

**ตัวอย่างผลลัพธ์:**
```
ค่าที่คาดหวัง: 5000
ค่าที่ได้จริง: 5000
ส่วนต่าง: 0
✅ ค่าถูกต้อง! Lock ทำงานได้ดี
```

---

### 4. เปรียบเทียบ (แนะนำ!)

```bash
python3 race_condition_comparison.py
```

**สิ่งที่จะเห็น:**
- เปรียบเทียบข้างเคียงกัน
- ความแตกต่างของเวลา
- ตารางสรุปผล

**ตัวอย่างผลลัพธ์:**
```
                         ไม่มี Lock      มี Lock
----------------------------------------------------
ค่าที่ได้                    4723         5000
ความถูกต้อง              ❌ ไม่ถูก       ✅ ถูก
เวลา (วินาที)               0.0234       0.0245
Race Condition             ❌ มี        ✅ ไม่มี
```

---

## 📊 การบันทึกผล

### สำหรับ Multi-threading (2.3)

**ข้อมูลที่ต้องบันทึก:**
- ชื่อโปรแกรม: `multi_threading_demo.py`
- จำนวน threads: 3 workers, 4 downloads
- สิ่งที่สังเกตได้:
  - Threads ทำงานพร้อมกัน
  - ลำดับการทำงานไม่แน่นอน
  - Thread IDs แตกต่างกัน
- ปัญหาที่พบ: (ถ้ามี)

### สำหรับ Race Condition (2.4)

**ข้อมูลที่ต้องบันทึก:**

**Problem Version:**
- ชื่อโปรแกรม: `race_condition_problem.py`
- ผลที่เกิด: ค่าไม่ถูกต้อง, สูญหาย X ครั้ง
- ทดสอบ 3 ครั้ง: ผลแต่ละครั้งต่างกัน

**Fixed Version:**
- ชื่อโปรแกรม: `race_condition_fixed.py`  
- ผลหลังแก้: ค่าถูกต้อง 100%
- วิธีแก้: ใช้ threading.Lock()

**สิ่งที่สังเกตได้:**
- Race Condition เกิดเมื่อหลาย threads เข้าถึงตัวแปรเดียวกัน
- Lock ช่วยป้องกัน แต่ทำให้ช้าขึ้นเล็กน้อย
- ความถูกต้องสำคัญกว่าความเร็ว

---

## 💡 เคล็ดลับ

### Multi-threading
1. **รันหลายครั้ง** - สังเกตลำดับที่เปลี่ยนไป
2. **ดู Thread ID** - แต่ละ thread มี ID ไม่ซ้ำ
3. **จับเวลา** - เปรียบเทียบกับ sequential

### Race Condition
1. **รัน Problem ก่อน** - ดูปัญหาชัดเจน
2. **บันทึกค่าที่สูญหาย** - แต่ละครั้งอาจต่างกัน
3. **รัน Fixed หลัง** - เห็นความแตกต่าง
4. **รัน Comparison** - เข้าใจภาพรวม

---

## ❓ คำถามที่ควรตอบได้

### Multi-threading
1. Threads ทำงานพร้อมกันจริงหรือไม่?
2. Thread ID คืออะไร?
3. ทำไมลำดับไม่แน่นอน?

### Race Condition
1. Race Condition คืออะไร?
2. ทำไมค่าถึงสูญหาย?
3. Lock ทำงานอย่างไร?
4. Trade-off ของการใช้ Lock คืออะไร?

---

## 🎯 เกณฑ์การประเมิน

### Multi-threading (2.3)
- รันโปรแกรมได้ (2 คะแนน)
- สังเกตการทำงานของ threads (2 คะแนน)
- ทดสอบหลายครั้งและบันทึกผล (1 คะแนน)

### Race Condition (2.4)
- รัน Problem version และเห็นปัญหา (2 คะแนน)
- รัน Fixed version และเห็นผลดี (2 คะแนน)
- เปรียบเทียบและอธิบายได้ (1 คะแนน)

---

## 📝 Template การบันทึก

```markdown
## 2.3 Multi-threading

ชื่อโปรแกรม: multi_threading_demo.py
จำนวน threads: 3 workers, 4 downloads

สิ่งที่สังเกตได้:
- [บันทึกสิ่งที่เห็น]
- [ลำดับการทำงาน]
- [Thread IDs]

ภาพหน้าจอ: [แนบ]
คะแนน: __/5

---

## 2.4 Race Condition

### Problem Version
ชื่อโปรแกรม: race_condition_problem.py
ผลที่เกิด: ค่าสูญหาย ___ ครั้ง (จาก ___ ครั้ง)

### Fixed Version  
ชื่อโปรแกรม: race_condition_fixed.py
ผลหลังแก้: ค่าถูกต้อง ___/___

### สิ่งที่สังเกต
- Race Condition เกิดเพราะ: [อธิบาย]
- วิธีแก้: [อธิบาย]
- Trade-off: [อธิบาย]

ภาพหน้าจอ: [แนบ]
คะแนน: __/5
```

---

**หมายเหตุ:**
- ใช้ Python 3.8 ขึ้นไป
- ไม่ต้องติดตั้ง package เพิ่ม
- รันใน Linux/Ubuntu ได้เลย
