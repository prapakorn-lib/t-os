# คู่มือการทำ LAB ส่วนที่ 2: การเขียนโปรแกรมและการจำลอง

## ข้อมูลทั่วไป
- **ส่วนที่:** 2
- **หัวข้อ:** การเขียนโปรแกรมและการจำลอง
- **คะแนนเต็ม:** 20 คะแนน
- **จำนวนหัวข้อ:** เลือกทำ 4 รายการ (2.1 และ 2.2 บังคับ + เลือกอีก 2)
- **เวลาโดยประมาณ:** 2-3 ชั่วโมง

---

## 📋 สารบัญ

1. [2.1 การพัฒนาโปรแกรมบนระบบปฏิบัติการ (บังคับ)](#21-การพัฒนาโปรแกรมบนระบบปฏิบัติการ-บังคับ)
2. [2.2 การเขียนโปรแกรมจัดการไฟล์ (บังคับ)](#22-การเขียนโปรแกรมจัดการไฟล์-บังคับ)
3. [2.3 การรันโปรแกรม Multi-threading (เลือก)](#23-การรันโปรแกรม-multi-threading-เลือก)
4. [2.4 การรันโปรแกรม Race Condition (เลือก)](#24-การรันโปรแกรม-race-condition-เลือก)
5. [2.5 การรันโปรแกรม CPU Scheduling (เลือก)](#25-การรันโปรแกรม-cpu-scheduling-เลือก)
6. [2.6 การรันโปรแกรม Memory Management (เลือก)](#26-การรันโปรแกรม-memory-management-เลือก)
7. [2.7 การเขียนโปรแกรมจัดการไฟล์และอุปกรณ์ I/O (เลือก)](#27-การเขียนโปรแกรมจัดการไฟล์และอุปกรณ์-io-เลือก)

---

## 2.1 การพัฒนาโปรแกรมบนระบบปฏิบัติการ (บังคับ)

### วัตถุประสงค์
- เรียนรู้การติดตั้งและใช้งาน Package Manager
- ติดตั้ง Development Tools ที่จำเป็น
- ตั้งค่า Environment Variables

### 2.1.1 การติดตั้ง Package Manager

#### ขั้นตอนการทำงาน

1. **Update Package Lists**
```bash
sudo apt update
```

2. **ติดตั้ง Package ตัวอย่าง**
```bash
sudo apt install curl wget git
```

3. **ตรวจสอบ Package ที่ติดตั้ง**
```bash
apt list --installed | grep curl
```

#### การบันทึกผล
- บันทึกคำสั่งที่ใช้
- บันทึกปัญหาที่พบและวิธีแก้ไข
- ประเมินคะแนนตัวเองตามเกณฑ์ Rubrics

---

### 2.1.2 การติดตั้ง Development Tools

#### เครื่องมือที่ต้องติดตั้ง

1. **GCC (C Compiler)**
```bash
sudo apt install build-essential
gcc --version
```

2. **Python และ pip**
```bash
sudo apt install python3 python3-pip
python3 --version
pip3 --version
```

3. **Git**
```bash
sudo apt install git
git --version
```

4. **Text Editor**
```bash
# เลือกติดตั้ง vim หรือ nano
sudo apt install vim
# หรือ
sudo apt install nano
```

#### การตรวจสอบ
```bash
# ตรวจสอบเวอร์ชันทั้งหมด
echo "Python: $(python3 --version)"
echo "GCC: $(gcc --version | head -n1)"
echo "pip: $(pip3 --version)"
echo "Git: $(git --version)"
```

---

### 2.1.3 การตั้งค่า Environment Variables

#### ขั้นตอนการทำงาน

1. **ดู Environment Variables ปัจจุบัน**
```bash
# ดู variables ทั้งหมด
printenv

# ดู PATH
echo $PATH

# ดู HOME
echo $HOME
```

2. **ตั้งค่า PATH variable ชั่วคราว**
```bash
# เพิ่ม directory ใหม่ใน PATH
export PATH=$PATH:/home/youruser/mybin

# ตรวจสอบ
echo $PATH
```

3. **ตั้งค่า PATH variable ถาวร**
```bash
# แก้ไขไฟล์ .bashrc
nano ~/.bashrc

# เพิ่มบรรทัดนี้ที่ท้ายไฟล์
export PATH=$PATH:/home/youruser/mybin

# บันทึกและออก (Ctrl+X, Y, Enter)

# โหลดค่าใหม่
source ~/.bashrc
```

#### การทดสอบ
```bash
# สร้าง script ทดสอบ
mkdir -p ~/mybin
echo '#!/bin/bash' > ~/mybin/hello.sh
echo 'echo "Hello from mybin!"' >> ~/mybin/hello.sh
chmod +x ~/mybin/hello.sh

# ทดสอบเรียกใช้
hello.sh
```

---

## 2.2 การเขียนโปรแกรมจัดการไฟล์ 

### วัตถุประสงค์
- เขียนโปรแกรมอ่าน/เขียนไฟล์
- จัดการ File Operations ต่างๆ
- เรียนรู้การจัดการข้อผิดพลาด (Error Handling)

### 2.2.1 การเขียนโปรแกรมอ่าน/เขียนไฟล์

#### โปรแกรมที่ 1: การอ่านไฟล์ (file_read.py)

**วัตถุประสงค์:** เปิดและอ่านเนื้อหาจากไฟล์

**ขั้นตอนการใช้งาน:**

1. สร้างไฟล์ทดสอบ:
```bash
echo "Hello, Operating Systems Lab!" > test_input.txt
echo "This is line 2" >> test_input.txt
echo "This is line 3" >> test_input.txt
```

2. รันโปรแกรม:
```bash
python3 file_read.py
```

3. ตรวจสอบผลลัพธ์

**คำอธิบาย Code:**
- ใช้ `with open()` เพื่อเปิดไฟล์อย่างปลอดภัย (จะปิดไฟล์อัตโนมัติ)
- ใช้ `try-except` จัดการข้อผิดพลาด
- แสดงเนื้อหาทั้งหมดและทีละบรรทัด

---

#### โปรแกรมที่ 2: การเขียนไฟล์ (file_write.py)

**วัตถุประสงค์:** สร้างไฟล์ใหม่และเขียนข้อมูลลงไฟล์

**ขั้นตอนการใช้งาน:**

1. รันโปรแกรม:
```bash
python3 file_write.py
```

2. ตรวจสอบไฟล์ที่สร้าง:
```bash
cat output.txt
```

**คำอธิบาย Code:**
- ใช้ mode `'w'` เพื่อเขียนไฟล์ (จะลบข้อมูลเดิม)
- ใช้ mode `'a'` เพื่อเพิ่มข้อมูลต่อท้าย
- ใช้ `write()` และ `writelines()` เขียนข้อมูล

---

#### โปรแกรมที่ 3: การอ่าน/เขียนไฟล์ (file_operations.py)

**วัตถุประสงค์:** แสดงการทำงานอ่าน/เขียนไฟล์พร้อม Error Handling

**ขั้นตอนการใช้งาน:**

1. รันโปรแกรม:
```bash
python3 file_operations.py
```

2. ดูผลลัพธ์และไฟล์ที่สร้าง

**คำอธิบาย Code:**
- แสดงการจัดการข้อผิดพลาดแบบครบถ้วน
- ใช้ `os.path.exists()` ตรวจสอบไฟล์ก่อนอ่าน
- แสดง System calls ที่เกิดขึ้น

---

### 2.2.2 การจัดการ File Operations 

#### โปรแกรมที่ 4: การจัดการไฟล์ครบวงจร (file_management.py)

**วัตถุประสงค์:** แสดงการทำงานของ File Operations ต่างๆ

**Operations ที่รวมอยู่:**
1. Create file - สร้างไฟล์ใหม่
2. Delete file - ลบไฟล์
3. Copy file - คัดลอกไฟล์
4. Move/Rename file - ย้าย/เปลี่ยนชื่อไฟล์
5. Check file existence - ตรวจสอบการมีอยู่ของไฟล์
6. Get file information - ดูข้อมูลไฟล์ (ขนาด, สิทธิ์)

**ขั้นตอนการใช้งาน:**

1. รันโปรแกรม:
```bash
python3 file_management.py
```

2. สังเกตผลลัพธ์แต่ละ operation

**คำอธิบาย Code:**
- ใช้โมดูล `os` และ `shutil` จัดการไฟล์
- แสดงข้อมูลไฟล์ด้วย `os.stat()`
- จัดการ permissions ด้วย `os.chmod()`

---

## 2.3 การรันโปรแกรม Multi-threading 

### วัตถุประสงค์
- เข้าใจการทำงานของ Multi-threading
- สังเกตการทำงานของ threads หลายตัวพร้อมกัน

### ขั้นตอนการทำงาน

**หมายเหตุ:** ส่วนนี้ใช้โปรแกรมที่อาจารย์ให้ ไม่ต้องเขียน code เอง

1. **ดาวน์โหลดโปรแกรม**
   - clone จาก repository

2. **รันโปรแกรม**
```bash
python3 multi_threading_demo.py
```

3. **สังเกตการทำงาน**
   - สังเกต thread ID
   - สังเกตลำดับการทำงาน
   - สังเกตเวลาที่ใช้

4. **ทดสอบหลายครั้ง**
```bash
# ทดสอบ 3-5 ครั้ง
for i in {1..5}; do
    echo "=== Run $i ==="
    python3 multi_threading_demo.py
    echo ""
done
```

### การบันทึกผล
- ชื่อโปรแกรม
- จำนวน threads ที่สร้าง
- สิ่งที่สังเกตได้
- ปัญหาที่พบ (ถ้ามี)

---

## 2.4 การรันโปรแกรม Race Condition 

### วัตถุประสงค์
- เข้าใจ Race Condition
- เห็นความแตกต่างระหว่างโปรแกรมที่มี Race Condition และที่แก้ไขแล้ว

### ขั้นตอนการทำงาน

**หมายเหตุ:** ส่วนนี้ใช้โปรแกรมที่อาจารย์ให้ ไม่ต้องเขียน code เอง

1. **รันโปรแกรมที่มี Race Condition**
```bash
python3 race_condition_problem.py
```
   - สังเกตผลลัพธ์ที่ไม่ถูกต้อง
   - บันทึกค่าที่ได้

2. **รันโปรแกรมที่แก้ไขแล้ว**
```bash
python3 race_condition_fixed.py
```
   - สังเกตผลลัพธ์ที่ถูกต้อง
   - เปรียบเทียบกับโปรแกรมก่อนหน้า

3. **เปรียบเทียบผลลัพธ์**
   - บันทึกความแตกต่าง
   - อธิบายว่าทำไมผลลัพธ์ต่างกัน

### การบันทึกผล
- ชื่อโปรแกรม
- ผลที่เกิดจาก Race Condition
- ผลหลังแก้ไข
- สิ่งที่สังเกตได้

---

## 2.5 การรันโปรแกรม CPU Scheduling 

### วัตถุประสงค์
- เข้าใจ CPU Scheduling Algorithms
- เปรียบเทียบประสิทธิภาพของ algorithms ต่างๆ

### ขั้นตอนการทำงาน

1. **รันโปรแกรม FCFS (First-Come, First-Served)**
```bash
python3 cpu_scheduling.py --algorithm fcfs
```

2. **รันโปรแกรม SJF (Shortest Job First)**
```bash
python3 cpu_scheduling.py --algorithm sjf
```

3. **รันโปรแกรม Round Robin**
```bash
python3 cpu_scheduling.py --algorithm rr --quantum 2
```

4. **เปรียบเทียบผลลัพธ์**
   - Waiting Time ของแต่ละ algorithm
   - Turnaround Time ของแต่ละ algorithm
   - Algorithm ใดทำงานเร็วที่สุด

### การบันทึกผล
- ชื่อโปรแกรม
- จำนวน processes ที่ทดสอบ
- Waiting time และ Turnaround time ของแต่ละ algorithm
- Algorithm ที่ทำงานเร็วที่สุด
- สิ่งที่สังเกตได้

---

## 2.6 การรันโปรแกรม Memory Management 

### วัตถุประสงค์
- เข้าใจ Memory Allocation Algorithms
- สังเกตการจัดสรร memory

### ขั้นตอนการทำงาน

1. **รันโปรแกรม Memory Allocation**
```bash
python3 memory_management.py
```

2. **ทดสอบกับค่า input ต่างๆ**
   - ลองเปลี่ยนขนาด memory
   - ลองเปลี่ยนขนาด processes

3. **สังเกตผลลัพธ์**
   - การจัดสรร memory
   - Fragmentation ที่เกิดขึ้น
   - Algorithm ที่ใช้ (First-Fit, Best-Fit, Worst-Fit)

### การบันทึกผล
- ชื่อโปรแกรม
- Algorithm ที่ทดสอบ
- Memory Size ที่ใช้
- Process Sizes ที่ทดสอบ
- ผลลัพธ์ที่สังเกตได้
- สิ่งที่สังเกตได้

---

## 2.7 การเขียนโปรแกรมจัดการไฟล์และอุปกรณ์ I/O 

### วัตถุประสงค์
- เข้าใจ File I/O Operations ขั้นสูง
- เรียนรู้การทำงานกับอุปกรณ์ I/O

### 2.7.1 File I/O Operations (เลือก 2 รายการ)

#### ตัวเลือกที่ 1: Sequential File Access

**โปรแกรม:** sequential_file_access.py

**ขั้นตอนการใช้งาน:**
1. รันโปรแกรม:
```bash
python3 sequential_file_access.py
```

2. สังเกตการอ่านไฟล์แบบเรียงลำดับ

**คำอธิบาย:**
- อ่านไฟล์ทีละบรรทัดตามลำดับ
- เหมาะสำหรับไฟล์ขนาดใหญ่

---

#### ตัวเลือกที่ 2: Random File Access

**โปรแกรม:** random_file_access.py

**ขั้นตอนการใช้งาน:**
1. รันโปรแกรม:
```bash
python3 random_file_access.py
```

2. สังเกตการอ่านไฟล์แบบสุ่ม

**คำอธิบาย:**
- ใช้ `seek()` เพื่อกระโดดไปยังตำแหน่งที่ต้องการ
- อ่านข้อมูลจากตำแหน่งใดๆ ก็ได้

---

#### ตัวเลือกที่ 3: Buffered I/O

**โปรแกรม:** buffered_io.py

**ขั้นตอนการใช้งาน:**
1. รันโปรแกรม:
```bash
python3 buffered_io.py
```

2. เปรียบเทียบความเร็วระหว่าง Buffered และ Unbuffered I/O

**คำอธิบาย:**
- ใช้ buffer เพื่อเพิ่มประสิทธิภาพ
- ลดจำนวนครั้งในการเข้าถึง disk

---

### 2.7.2 Device I/O Programming 

#### ตัวเลือกที่ 1: Keyboard Input

**โปรแกรม:** keyboard_input.py

**ขั้นตอนการใช้งาน:**
1. รันโปรแกรม:
```bash
python3 keyboard_input.py
```

2. ทดสอบ input ต่างๆ

---

#### ตัวเลือกที่ 2: File I/O with Binary

**โปรแกรม:** binary_file_io.py

**ขั้นตอนการใช้งาน:**
1. รันโปรแกรม:
```bash
python3 binary_file_io.py
```

2. สังเกตการอ่าน/เขียนไฟล์ binary

---

## 📊 การประเมินผล

### เกณฑ์การให้คะแนน (Rubrics Score)

| คะแนน | ระดับความสำเร็จ | รายละเอียด |
|:---:|:---:|---|
| **5** | ⭐⭐⭐ ยอดเยี่ยม | ทำได้สำเร็จสมบูรณ์ในครั้งแรก ไม่มีข้อผิดพลาด มีความเข้าใจชัดเจน |
| **4** | ⭐⭐ ดีมาก | ทำได้สำเร็จสมบูรณ์ แต่ต้องแก้ไขเล็กน้อย (1-2 ครั้ง) เข้าใจหลักการ |
| **3** | ⭐ ดี | ทำได้สำเร็จบางขั้นตอน หรือต้องแก้ไขหลายครั้ง (3-4 ครั้ง) |
| **2** | ⚠️ พอใช้ | ทำได้บางส่วน ต้องการความช่วยเหลือ หรือมีปัญหาหลายจุด |
| **1** | ❌ ต้องปรับปรุง | ไม่สามารถทำได้ หรือทำไม่สำเร็จ ต้องทำใหม่ |
| **0** | - | ไม่ได้ทำ |

---

## 📝 การบันทึกผล

### ตารางสรุปคะแนนส่วนที่ 2

| รายการ | คะแนนที่ได้ | คะแนนเต็ม |
|---|:---:|:---:|
| 2.1 การพัฒนาโปรแกรมบน OS | ___ | 5 |
| 2.2 การเขียนโปรแกรมจัดการไฟล์ | ___ | 5 |
| 2.x รายการที่เลือก 1 | ___ | 5 |
| 2.x รายการที่เลือก 2 | ___ | 5 |
| **รวมส่วนที่ 2** | **___** | **20** |

---

## 💡 เคล็ดลับและข้อควรระวัง

### สำหรับการเขียนโปรแกรม

1. **ใช้ Virtual Environment** (แนะนำ)
```bash
python3 -m venv venv
source venv/bin/activate
```

2. **จัดการ Error อย่างเหมาะสม**
   - ใช้ try-except เสมอ
   - แสดง error message ที่มีประโยชน์

3. **ตั้งชื่อตัวแปรให้ชัดเจน**
   - ใช้ชื่อที่อธิบายตัวเอง (descriptive)
   - ตาม PEP 8 style guide

4. **เขียน Comment**
   - อธิบายส่วนที่ซับซ้อน
   - ระบุ System calls ที่ใช้

### สำหรับการรันโปรแกรม

1. **อ่านคำแนะนำให้ละเอียด**
   - ดู parameters ที่ต้องการ
   - เข้าใจผลลัพธ์ที่คาดหวัง

2. **ทดสอบหลายครั้ง**
   - ใช้ input ที่แตกต่างกัน
   - บันทึกผลแต่ละครั้ง

3. **บันทึกปัญหาและวิธีแก้**
   - เก็บ error messages
   - จดวิธีแก้ไขที่ได้ผล

---

## 📚 เอกสารอ้างอิง

### Python Documentation
- [Python File I/O](https://docs.python.org/3/tutorial/inputoutput.html)
- [Python os module](https://docs.python.org/3/library/os.html)
- [Python shutil module](https://docs.python.org/3/library/shutil.html)

### Linux Documentation
- [Linux System Calls](https://man7.org/linux/man-pages/man2/syscalls.2.html)
- [File Permissions](https://www.linux.com/training-tutorials/understanding-linux-file-permissions/)

### Operating Systems Concepts
- Silberschatz - Chapter 2: Operating System Structures
- Silberschatz - Chapter 3: Processes
- Silberschatz - Chapter 11: File-System Interface

---

## ❓ คำถามที่พบบ่อย (FAQ)

### Q1: ควรเลือกทำหัวข้อใดเพิ่มจาก 2.3-2.7?
**A:** แนะนำเลือกตามความสนใจและเป้าหมายการเรียนรู้:
- สนใจ Concurrency → เลือก 2.3 Multi-threading
- สนใจ Synchronization → เลือก 2.4 Race Condition
- สนใจ Algorithms → เลือก 2.5 CPU Scheduling หรือ 2.6 Memory Management
- สนใจ I/O → เลือก 2.7 File and Device I/O

### Q2: Python version ไหนที่เหมาะสม?
**A:** แนะนำ Python 3.8 ขึ้นไป (Python 3.9 หรือ 3.10 ดีที่สุด)

### Q3: รันโปรแกรมไม่ได้ ต้องทำอย่างไร?
**A:** ตรวจสอบตามลำดับ:
1. ตรวจสอบ Python version
2. ตรวจสอบ permissions (chmod +x)
3. ตรวจสอบ error message
4. ดูที่ระบุไว้ใน error

### Q4: ต้องใช้ Virtual Environment หรือไม่?
**A:** ไม่จำเป็น แต่แนะนำให้ใช้เพื่อความเป็นระเบียบ

---

## 📋 Checklist ก่อนส่งงาน

- [ ] ทำครบ 4 รายการ (2.1, 2.2 และอีก 2 รายการที่เลือก)
- [ ] ทดสอบโปรแกรมทั้งหมดแล้ว
- [ ] บันทึกผลลัพธ์และ screenshot สำคัญ
- [ ] เขียนสรุปปัญหาและวิธีแก้ไข
- [ ] ประเมินคะแนนตัวเองตาม Rubrics
- [ ] เก็บ Code ทั้งหมดไว้ใน folder เดียวกัน
- [ ] ตรวจสอบความครบถ้วนของเอกสาร

---

**จัดทำโดย:** Prapakorn Srisawangwong
**วันที่:** November 2025  
**เวอร์ชัน:** 1.0

---

# สิ้นสุดคู่มือส่วนที่ 2
