# 📚 คู่มือและโค้ด Lab ส่วนที่ 2

## 📁 ไฟล์ที่มีในชุดนี้

### 📖 เอกสารคู่มือ
- **lab2_guide.md** - คู่มือการทำ Lab ส่วนที่ 2 ฉบับสมบูรณ์

### 💻 โค้ด Python (ส่วนบังคับ)

#### 2.2 การเขียนโปรแกรมจัดการไฟล์
1. **file_read.py** - โปรแกรมอ่านไฟล์
2. **file_write.py** - โปรแกรมเขียนไฟล์
3. **file_operations.py** - โปรแกรมอ่าน/เขียนแบบครบวงจร
4. **file_management.py** - โปรแกรมจัดการไฟล์ครบวงจร (Create, Delete, Copy, Move, etc.)

### 💻 โค้ด Python (ส่วนเลือก - 2.3, 2.4)

#### 2.3 Multi-threading
1. **multi_threading_demo.py** - แสดงการทำงานของ threads หลายตัว

#### 2.4 Race Condition
1. **race_condition_problem.py** - โปรแกรมที่มี Race Condition (บั๊ก)
2. **race_condition_fixed.py** - โปรแกรมที่แก้ไข Race Condition ด้วย Lock
3. **race_condition_comparison.py** - เปรียบเทียบมี/ไม่มี Lock

### 💻 โค้ด Python (ส่วนเลือก - 2.7)

#### 2.7.1 File I/O Operations
1. **sequential_file_access.py** - Sequential File Access
2. **random_file_access.py** - Random File Access
3. **buffered_io.py** - Buffered I/O

#### 2.7.2 Device I/O Programming
1. **keyboard_input.py** - Keyboard Input
2. **binary_file_io.py** - Binary File I/O

---

## 🚀 วิธีการใช้งาน

### ขั้นตอนที่ 1: เตรียมสิ่งแวดล้อม

```bash
# ตรวจสอบ Python version
python3 --version

# สร้าง directory สำหรับ lab
mkdir -p ~/os_lab/part2
cd ~/os_lab/part2

# คัดลอกไฟล์ทั้งหมด
# (คัดลอกไฟล์ทั้งหมดที่ได้รับมา)
```

### ขั้นตอนที่ 2: รันโปรแกรม

#### ส่วนบังคับ (2.1, 2.2)

```bash
# 2.2.1 อ่าน/เขียนไฟล์
python3 file_read.py
python3 file_write.py
python3 file_operations.py

# 2.2.2 จัดการไฟล์
python3 file_management.py
```

#### ส่วนเลือก (2.3, 2.4, 2.7)

```bash
# 2.3 Multi-threading
python3 multi_threading_demo.py

# 2.4 Race Condition
python3 race_condition_problem.py       # ดูปัญหา
python3 race_condition_fixed.py         # ดูวิธีแก้
python3 race_condition_comparison.py    # เปรียบเทียบ

# 2.7 File I/O Operations
python3 sequential_file_access.py
python3 random_file_access.py
python3 buffered_io.py

# Device I/O
python3 keyboard_input.py
python3 binary_file_io.py
```

---

## 📝 การบันทึกผล

### สำหรับแต่ละโปรแกรม ให้บันทึก:

1. **Screenshot** - ภาพหน้าจอการรันโปรแกรม
2. **ปัญหาที่พบ** - บันทึกปัญหาและวิธีแก้ไข
3. **System Calls** - บันทึก System Calls ที่ใช้
4. **คะแนนตัวเอง** - ประเมินตาม Rubrics (0-5)

### ตัวอย่างการบันทึก:

```
โปรแกรม: file_read.py
วันที่รัน: 4 พ.ย. 2568
ผลลัพธ์: สำเร็จ
ปัญหา: ไม่มี
System Calls: open(), read(), close()
คะแนน: 5/5 (ยอดเยี่ยม)
```

---

## 🎯 เกณฑ์การให้คะแนน (Rubrics)

| คะแนน | ระดับ | คำอธิบาย |
|:---:|:---:|---|
| 5 | ⭐⭐⭐ | ทำสำเร็จครั้งแรก ไม่มี error |
| 4 | ⭐⭐ | สำเร็จ แก้ไข 1-2 ครั้ง |
| 3 | ⭐ | สำเร็จบางส่วน แก้ไข 3-4 ครั้ง |
| 2 | ⚠️ | ต้องการความช่วยเหลือ |
| 1 | ❌ | ไม่สำเร็จ |

---

## 💡 เคล็ดลับ

1. **อ่านคู่มือก่อนทำ** - เปิดไฟล์ lab2_guide.md
2. **ทดสอบทีละโปรแกรม** - อย่ารันพร้อมกันหมด
3. **บันทึกทันที** - บันทึกผลขณะทำ
4. **เก็บ Code** - backup ไฟล์ที่สำคัญ

---

## ❓ การแก้ปัญหาทั่วไป

### ปัญหา: Permission Denied
```bash
chmod +x *.py
```

### ปัญหา: Python not found
```bash
# ใช้ python3 แทน python
python3 file_read.py
```

### ปัญหา: Encoding Error
- เปลี่ยน encoding เป็น utf-8 ในโค้ด
- ตรวจสอบ locale ของระบบ

---

## 📊 สรุปส่วนที่ 2

| รายการ | จำนวน | คะแนนเต็ม |
|---|:---:|:---:|
| ส่วนบังคับ (2.1, 2.2) | 2 | 10 |
| ส่วนเลือก (เลือก 2 จาก 2.3-2.7) | 2 | 10 |
| **รวม** | **4** | **20** |

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย:
- ตรวจสอบคู่มือ lab2_guide.md
- ดู error message ใน terminal
- ทดสอบในเครื่องที่มี Python 3.8+

---

**จัดทำโดย:** Operating Systems Lab  
**วันที่:** November 2025  
**เวอร์ชัน:** 1.0

---

**หมายเหตุ:**
- ไฟล์ทั้งหมดทดสอบแล้วบน Ubuntu 22.04 LTS
- Python 3.8 ขึ้นไป
- ไม่ต้องติดตั้ง package เพิ่มเติม (ใช้ standard library)
